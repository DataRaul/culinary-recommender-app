# Corpus Scale Step 8G — CC0 Markdown v8003 Prewrite

Status: **NO-WRITE PREFLIGHT / STEP 8F PARKED**

Date: **2026-09-15**

The preceding measurement of pinned `sylGauthier/recipes@b12e481d1c220a13e0847a34e148d5872a16928e` earned `STEP_8G_CC0_MARKDOWN_MEASUREMENT_EARNED_COHORT_CANDIDATE` for protected-scale testing only.

This prewrite tests the exact next immutable layer:

- active parent: `v8002` = 501 UniTools + 915 ForkRecipe = 1,416 protected recipes;
- proposed child: `v8003` = 226 pinned CC0 Markdown recipes;
- proposed composed protected corpus: 1,642 recipes;
- existing topology: exactly two recipe-body shards;
- prior bodies remain immutable and are not copied into `v8003`;
- route index is extended to exact `(recipe_id -> corpus_version + shard)` routing;
- full-corpus scans remain forbidden.

The source packet preserves the exact raw Markdown plus pinned repository/commit/path/content fingerprint. Any parsed ingredients, directions or tags are explicitly non-authoritative source metadata and do not become canonical ingredient, nutrition, dietary/allergen or scaling authority.

A PASS earns only implementation of the authenticated live protected-population path for this exact `v8003` layer on the existing two-shard topology. It does not itself perform a D1 write.

Hard boundaries remain unchanged: Step 8F stays parked; public recommendations are unchanged; no third shard or billing expansion is authorized; Nutrition and YT-CUL are not mutated; Knowledge Core is read-only from this lane.
