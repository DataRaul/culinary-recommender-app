import { STEP8G_V8004_RUNTIME_DESCRIPTOR as descriptor } from "../../data/generated/step8g/v8004-runtime-descriptor.mjs";
import {
  STEP8B_LIVE_SHARD_SPECS,
  STEP8B_RECEIPT_TABLE,
  STEP8B_RECEIPT_TABLE_SQL,
  STEP8B_RECIPE_TABLE,
  STEP8B_RECIPE_TABLE_SQL,
  sha256Hex
} from "./step8b-live.mjs";
import {
  STEP8G_POINTER_SCOPE,
  STEP8G_POINTER_TABLE,
  STEP8G_POINTER_TABLE_SQL,
  STEP8G_ROUTE_RECEIPT_TABLE,
  STEP8G_ROUTE_RECEIPT_TABLE_SQL,
  STEP8G_ROUTE_TABLE,
  STEP8G_ROUTE_TABLE_SQL,
  readStep8GPointer
} from "./step8g-live-runtime.mjs";
import { STEP8G_V8003_RUNTIME_DESCRIPTOR } from "../../data/generated/step8g/v8003-runtime-descriptor.mjs";

const encoder = new TextEncoder();
export const STEP8G_V8004_CORPUS_VERSION = "v8004";
export const STEP8G_V8004_PARENT_VERSION = "v8003";
export const STEP8G_V8004_SOURCE_COHORT_ID = "ORA_ABBOTT_1864_AE3BD2C";
export const STEP8G_V8004_EXPECTED_RECIPE_COUNT = 713;
export const STEP8G_V8004_EXPECTED_BODY_BATCH_COUNT = 73;
export const STEP8G_V8004_EXPECTED_ROUTE_COUNT = 2355;
export const STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT = 1642;
export const STEP8G_V8004_MAX_ROWS_PER_BATCH = 10;
export const STEP8G_V8004_MAX_PROTECTED_D1_SUBQUERIES = 16;
export const STEP8G_V8004_LIVE_SHARD_SPECS = STEP8B_LIVE_SHARD_SPECS;

const SOURCE_REPOSITORY = "AdamBouhmad/open-recipe-archive";
const SOURCE_TITLE = "The English and Australian Cookery Book";
const SOURCE_AUTHOR = "Edward Abbott";
const SOURCE_YEAR = "1864";
const SOURCE_URL = "https://archive.org/details/b21505524";

const hashes = descriptor.bodyHashHexByShard.map(value => value.match(/.{64}/g) || []);
const bodyBatches = hashes.flatMap((list, shardNumber) => list.map((expectedSha256, batchNumber) => {
  const remaining = Number(descriptor.bodyShardRows[shardNumber]) - batchNumber * STEP8G_V8004_MAX_ROWS_PER_BATCH;
  return {
    batchId: `s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,
    shardNumber,
    batchNumber,
    rowCount: Math.min(STEP8G_V8004_MAX_ROWS_PER_BATCH, remaining),
    expectedSha256
  };
}));
const routeBatches = descriptor.bodyShardRows.flatMap((rowCount, shardNumber) =>
  Array.from({length:Math.ceil(Number(rowCount)/STEP8G_V8004_MAX_ROWS_PER_BATCH)},(_,batchNumber)=>({
    batchId:`route-v8004-s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,
    shardNumber,
    batchNumber,
    rowCount:Math.min(STEP8G_V8004_MAX_ROWS_PER_BATCH,Number(rowCount)-batchNumber*STEP8G_V8004_MAX_ROWS_PER_BATCH)
  }))
);
const bodyBatchById = new Map(bodyBatches.map(batch => [batch.batchId,batch]));
const routeBatchById = new Map(routeBatches.map(batch => [batch.batchId,batch]));

const bytes = value => encoder.encode(String(value)).byteLength;
const bodyDescriptor = entry => ({ordinal:Number(entry.ordinal),recipeId:String(entry.recipeId),bodySha256:String(entry.bodySha256),bodyBytes:Number(entry.bodyBytes),sourceCohortId:String(entry.sourceCohortId)});
const routeDescriptor = entry => ({recipeId:String(entry.recipeId),corpusVersion:String(entry.corpusVersion),shardNumber:Number(entry.shardNumber),sourceCohortId:String(entry.sourceCohortId),bodySha256:String(entry.bodySha256),bodyBytes:Number(entry.bodyBytes)});

function frontMatter(markdown){const match=/^---\s*\n([\s\S]*?)\n---\s*(?:\n|$)/.exec(String(markdown??""));if(!match)return{};const meta={};for(const line of match[1].split(/\r?\n/)){const field=/^([a-zA-Z0-9_]+):\s*(.*)$/.exec(line);if(field)meta[field[1]]=field[2].trim().replace(/^(["'])(.*)\1$/,"$2");}return meta;}
function section(markdown,headingPattern){const pattern=new RegExp(`^##\\s+${headingPattern}\\s*$`,"im");const match=pattern.exec(markdown);if(!match)return"";const rest=markdown.slice(match.index+match[0].length);const next=/^##\s+/m.exec(rest);return(next?rest.slice(0,next.index):rest).trim();}
function parseSource(markdown){const text=String(markdown??"");const meta=frontMatter(text);const ingredients=section(text,"Ingredients").split(/\r?\n/).filter(line=>/^\s*[-*+]\s+/.test(line)).map(line=>line.replace(/^\s*[-*+]\s+/,"").trim()).filter(Boolean);const directions=section(text,"(?:Directions|Instructions|Method)").split(/\r?\n/).filter(line=>/^\s*(?:\d+[.)]|[-*+])\s+/.test(line)).map(line=>line.replace(/^\s*(?:\d+[.)]|[-*+])\s+/,"").trim()).filter(Boolean);return{meta,ingredients,directions};}
function slugFromFile(fileName){return String(fileName||"").replace(/\.md$/i,"").normalize("NFKD").replace(/\p{M}+/gu,"").toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_+|_+$/g,"");}
async function buildBody(rawMarkdown,fileName){const parsed=parseSource(rawMarkdown);const m=parsed.meta;if(m.source_title!==SOURCE_TITLE||m.author!==SOURCE_AUTHOR||m.source_year!==SOURCE_YEAR||m.source_url!==SOURCE_URL||m.license!=="public-domain"||!m.title||!parsed.ingredients.length||!parsed.directions.length)throw new Error("SOURCE_RECORD_RIGHTS_OR_STRUCTURE_MISMATCH");const recipeId=`ora_abbott_1864_${slugFromFile(fileName)}`;if(!recipeId||recipeId==="ora_abbott_1864_")throw new Error("SOURCE_RECIPE_ID_INVALID");const packet={schema:"STEP8G_ORA_ABBOTT_1864_PROTECTED_SOURCE_PACKET_V1",canonicalRecipeId:recipeId,source:{cohortId:STEP8G_V8004_SOURCE_COHORT_ID,repository:SOURCE_REPOSITORY,commit:descriptor.sourceCommit,path:`collections/australian-table/recipes/${fileName}`,sourceContentSha256:await sha256Hex(rawMarkdown),sourceWork:SOURCE_TITLE,sourceAuthor:SOURCE_AUTHOR,sourceYear:SOURCE_YEAR,sourceUrl:SOURCE_URL,licenseId:"public-domain"},sourceContent:{rawMarkdown,title:m.title,parsedIngredientsNonAuthoritative:parsed.ingredients,parsedDirectionsNonAuthoritative:parsed.directions},authority:{recommendationAdmissionAuthorized:false,publicRuntimeActivationAuthorized:false,ingredientOntologyAuthority:false,nutritionAuthority:false,dietaryAllergenAuthority:false,scalingAuthority:false,knowledgeCoreWriteAuthorized:false}};const bodyJson=JSON.stringify(packet);return{recipeId,bodyJson,bodySha256:await sha256Hex(bodyJson),bodyBytes:bytes(bodyJson),sourceCohortId:STEP8G_V8004_SOURCE_COHORT_ID};}

export function publicStep8GV8004Summary(){return{contractVersion:"CORPUS_SCALE_STEP8G_ORA_ABBOTT_V8004_LIVE_V1",corpusVersion:STEP8G_V8004_CORPUS_VERSION,parentCorpusVersion:STEP8G_V8004_PARENT_VERSION,sourceCohortId:STEP8G_V8004_SOURCE_COHORT_ID,sourceCommit:descriptor.sourceCommit,recipeCount:STEP8G_V8004_EXPECTED_RECIPE_COUNT,cumulativeRecipeCount:STEP8G_V8004_EXPECTED_ROUTE_COUNT,bodyBatchCount:bodyBatches.length,newRouteBatchCount:routeBatches.length,shardCount:2,maxRowsPerBatch:STEP8G_V8004_MAX_ROWS_PER_BATCH,maxProtectedD1Subqueries:STEP8G_V8004_MAX_PROTECTED_D1_SUBQUERIES,layerManifestSha256:descriptor.layerManifestSha256,populationPlanSha256:descriptor.populationPlanSha256,publicRuntimeActivationAuthorized:false,recommendationAdmissionAuthorized:false,billingExpansionAuthorized:false,thirdShardAuthorized:false};}
export const expectedStep8GV8004BodyBatchIds=()=>bodyBatches.map(b=>b.batchId);
export const expectedStep8GV8004RouteBatchIds=()=>routeBatches.map(b=>b.batchId);
export function publicStep8GV8004BodyBatch(id){const b=bodyBatchById.get(String(id||""));return b?{...b}:null;}
export function publicStep8GV8004RouteBatch(id){const b=routeBatchById.get(String(id||""));return b?{...b}:null;}

export async function materializeStep8GV8004IncomingBodyBatch(payload={}){const expected=bodyBatchById.get(String(payload.batchId||""));if(!expected)return{pass:false,reason:"UNKNOWN_BODY_BATCH_ID"};if(!Array.isArray(payload.sourceEntries)||payload.sourceEntries.length!==expected.rowCount)return{pass:false,reason:"BODY_BATCH_ROW_COUNT_MISMATCH"};const entries=[];const ids=new Set();for(const incoming of payload.sourceEntries){const ordinal=Number(incoming?.ordinal);if(!Number.isInteger(ordinal)||ordinal<0||ordinal>=STEP8G_V8004_EXPECTED_RECIPE_COUNT)return{pass:false,reason:"SOURCE_ORDINAL_INVALID"};let built;try{built=await buildBody(String(incoming?.rawMarkdown||""),String(incoming?.fileName||""));}catch(error){return{pass:false,reason:error.message||"SOURCE_RECORD_REJECTED"};}if(ids.has(built.recipeId))return{pass:false,reason:"DUPLICATE_SOURCE_RECIPE_ID"};ids.add(built.recipeId);entries.push({ordinal,...built});}const fingerprint={shardNumber:expected.shardNumber,batchNumber:expected.batchNumber,entries:entries.map(bodyDescriptor)};const observed=await sha256Hex(JSON.stringify(fingerprint));if(observed!==expected.expectedSha256)return{pass:false,reason:"BODY_BATCH_FINGERPRINT_MISMATCH"};return{pass:true,batch:{...expected,entries}};}

export async function ensureStep8GV8004ShardSchema(db){await db.prepare(STEP8B_RECIPE_TABLE_SQL).run();await db.prepare(STEP8B_RECEIPT_TABLE_SQL).run();return{initialized:true,d1Subqueries:2};}
export async function initializeStep8GV8004ControlSchema(db){await db.prepare(STEP8G_POINTER_TABLE_SQL).run();await db.prepare(STEP8G_ROUTE_TABLE_SQL).run();await db.prepare(STEP8G_ROUTE_RECEIPT_TABLE_SQL).run();return{initialized:true,d1Subqueries:3};}

async function verifyBodyRows(db,batch){const ids=batch.entries.map(e=>e.recipeId),ph=ids.map(()=>"?").join(",");const rows=await db.prepare(`SELECT ordinal,recipe_id,body_bytes,body_sha256,source_cohort_id FROM ${STEP8B_RECIPE_TABLE} WHERE corpus_version=? AND recipe_id IN (${ph})`).bind(STEP8G_V8004_CORPUS_VERSION,...ids).all();const byId=new Map((rows?.results||[]).map(r=>[String(r.recipe_id),r]));const pass=byId.size===batch.entries.length&&batch.entries.every(e=>{const r=byId.get(e.recipeId);return r&&Number(r.ordinal)===e.ordinal&&Number(r.body_bytes)===e.bodyBytes&&String(r.body_sha256)===e.bodySha256&&String(r.source_cohort_id)===e.sourceCohortId;});return{pass,rowCount:byId.size,d1Subqueries:1};}
export async function writeStep8GV8004BodyBatch(db,batch){const prior=await db.prepare(`SELECT expected_sha256,row_count,verified FROM ${STEP8B_RECEIPT_TABLE} WHERE corpus_version=? AND batch_id=? LIMIT 1`).bind(STEP8G_V8004_CORPUS_VERSION,batch.batchId).first();let q=1;if(prior){if(String(prior.expected_sha256)!==batch.expectedSha256||Number(prior.row_count)!==batch.rowCount)return{pass:false,status:"BODY_RECEIPT_CONFLICT",d1Subqueries:q};const rows=await verifyBodyRows(db,batch);q++;if(!rows.pass)return{pass:false,status:"BODY_RECEIPT_ROW_MISMATCH",d1Subqueries:q};if(Number(prior.verified)===1)return{pass:true,skipped:true,status:"VERIFIED_IDEMPOTENT_SKIP",rowCount:rows.rowCount,d1Subqueries:q};const promoted=await db.prepare(`UPDATE ${STEP8B_RECEIPT_TABLE} SET verified=1 WHERE corpus_version=? AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(STEP8G_V8004_CORPUS_VERSION,batch.batchId,batch.expectedSha256,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"RECOVERED_UNKNOWN_COMMIT_AND_VERIFIED",rowCount:rows.rowCount,d1Subqueries:q};}if(typeof db.batch!=="function")return{pass:false,status:"D1_BATCH_UNAVAILABLE",d1Subqueries:q};const statements=batch.entries.map(e=>db.prepare(`INSERT OR ABORT INTO ${STEP8B_RECIPE_TABLE} (corpus_version,ordinal,recipe_id,body_json,body_bytes,body_sha256,source_cohort_id) VALUES (?,?,?,?,?,?,?)`).bind(STEP8G_V8004_CORPUS_VERSION,e.ordinal,e.recipeId,e.bodyJson,e.bodyBytes,e.bodySha256,e.sourceCohortId));statements.push(db.prepare(`INSERT OR ABORT INTO ${STEP8B_RECEIPT_TABLE} (corpus_version,batch_id,expected_sha256,row_count,verified) VALUES (?,?,?,?,0)`).bind(STEP8G_V8004_CORPUS_VERSION,batch.batchId,batch.expectedSha256,batch.rowCount));try{await db.batch(statements);}catch{return{pass:false,status:"WRITE_ERROR_UNKNOWN_COMMIT_STATE",d1Subqueries:q+statements.length};}q+=statements.length;const rows=await verifyBodyRows(db,batch);q++;if(!rows.pass)return{pass:false,status:"POST_WRITE_ROW_VERIFICATION_MISMATCH",d1Subqueries:q};const promoted=await db.prepare(`UPDATE ${STEP8B_RECEIPT_TABLE} SET verified=1 WHERE corpus_version=? AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(STEP8G_V8004_CORPUS_VERSION,batch.batchId,batch.expectedSha256,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"WRITTEN_AND_EXACTLY_VERIFIED",rowCount:rows.rowCount,d1Subqueries:q};}

export async function readStep8GV8004BodyProgress(shardDbs){const completed=[],pending=[],conflicts=[];let q=0;for(const spec of STEP8G_V8004_LIVE_SHARD_SPECS){const rows=await shardDbs[spec.shardNumber].prepare(`SELECT batch_id,expected_sha256,row_count,verified FROM ${STEP8B_RECEIPT_TABLE} WHERE corpus_version=? ORDER BY batch_id`).bind(STEP8G_V8004_CORPUS_VERSION).all();q++;for(const row of rows?.results||[]){const id=String(row.batch_id||""),expected=bodyBatchById.get(id);if(!expected||expected.shardNumber!==spec.shardNumber||String(row.expected_sha256)!==expected.expectedSha256||Number(row.row_count)!==expected.rowCount)conflicts.push({batchId:id,reason:"BODY_RECEIPT_MISMATCH"});else if(Number(row.verified)===1)completed.push(id);else pending.push(id);}}const seen=new Set([...completed,...pending]);const missing=bodyBatches.map(b=>b.batchId).filter(id=>!seen.has(id));const verifiedRowCount=completed.reduce((sum,id)=>sum+bodyBatchById.get(id).rowCount,0);return{pass:conflicts.length===0,completeVerified:conflicts.length===0&&!pending.length&&!missing.length&&completed.length===STEP8G_V8004_EXPECTED_BODY_BATCH_COUNT&&verifiedRowCount===STEP8G_V8004_EXPECTED_RECIPE_COUNT,completedBatchIds:completed.sort(),pendingVerificationBatchIds:pending.sort(),missingBatchIds:missing,conflicts,verifiedBatchCount:completed.length,verifiedRowCount,expectedBatchCount:STEP8G_V8004_EXPECTED_BODY_BATCH_COUNT,expectedRecipeCount:STEP8G_V8004_EXPECTED_RECIPE_COUNT,fullCorpusScans:0,d1Subqueries:q};}

export async function copyStep8GV8004ParentRoutes(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(pointer.activeVersion!==STEP8G_V8004_PARENT_VERSION)return{pass:false,status:"V8003_PARENT_NOT_ACTIVE",activeVersion:pointer.activeVersion,d1Subqueries:q};const parent=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8003'`).first();q++;if(Number(parent?.c||0)!==STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT)return{pass:false,status:"V8003_ROUTE_BASELINE_NOT_EXACT",rowCount:Number(parent?.c||0),d1Subqueries:q};const existing=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8004' AND corpus_version IN ('v8001','v8002','v8003')`).first();q++;if(Number(existing?.c||0)===STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT)return{pass:true,skipped:true,status:"PARENT_ROUTES_IDEMPOTENT_SKIP",rowCount:STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT,d1Subqueries:q};if(Number(existing?.c||0)!==0)return{pass:false,status:"PARENT_ROUTE_PARTIAL_CONFLICT",rowCount:Number(existing?.c||0),d1Subqueries:q};await controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_TABLE} (composition_version,recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes) SELECT 'v8004',recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8003'`).run();q++;const after=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8004' AND corpus_version IN ('v8001','v8002','v8003')`).first();q++;const count=Number(after?.c||0);return{pass:count===STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT,status:count===STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT?"PARENT_ROUTES_COPIED_AND_VERIFIED":"PARENT_ROUTE_COPY_MISMATCH",rowCount:count,d1Subqueries:q};}

async function verifyRoutes(controlDb,entries){const ids=entries.map(e=>e.recipeId),ph=ids.map(()=>"?").join(",");const rows=await controlDb.prepare(`SELECT recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8004' AND recipe_id IN (${ph})`).bind(...ids).all();const byId=new Map((rows?.results||[]).map(r=>[String(r.recipe_id),r]));const pass=byId.size===entries.length&&entries.every(e=>{const r=byId.get(e.recipeId);return r&&String(r.corpus_version)===e.corpusVersion&&Number(r.shard_number)===e.shardNumber&&String(r.source_cohort_id)===e.sourceCohortId&&String(r.body_sha256)===e.bodySha256&&Number(r.body_bytes)===e.bodyBytes;});return{pass,rowCount:byId.size,d1Subqueries:1};}
export async function writeStep8GV8004RouteBatch(controlDb,shardDbs,payload={}){const batch=routeBatchById.get(String(payload.batchId||""));if(!batch)return{pass:false,status:"UNKNOWN_ROUTE_BATCH_ID",d1Subqueries:0};if(!Array.isArray(payload.entries)||payload.entries.length!==batch.rowCount)return{pass:false,status:"ROUTE_BATCH_ROW_COUNT_MISMATCH",d1Subqueries:0};const entries=payload.entries.map(routeDescriptor);const ids=entries.map(e=>e.recipeId);if(new Set(ids).size!==ids.length||JSON.stringify(ids)!==JSON.stringify([...ids].sort())||entries.some(e=>e.corpusVersion!==STEP8G_V8004_CORPUS_VERSION||e.shardNumber!==batch.shardNumber||!e.recipeId||e.sourceCohortId!==STEP8G_V8004_SOURCE_COHORT_ID||!/^[0-9a-f]{64}$/.test(e.bodySha256)||e.bodyBytes<=0))return{pass:false,status:"ROUTE_BATCH_DESCRIPTOR_INVALID",d1Subqueries:0};const ph=ids.map(()=>"?").join(",");const bodies=await shardDbs[batch.shardNumber].prepare(`SELECT recipe_id,body_sha256,body_bytes,source_cohort_id FROM ${STEP8B_RECIPE_TABLE} WHERE corpus_version='v8004' AND recipe_id IN (${ph})`).bind(...ids).all();let q=1;const byId=new Map((bodies?.results||[]).map(r=>[String(r.recipe_id),r]));if(byId.size!==entries.length||!entries.every(e=>{const r=byId.get(e.recipeId);return r&&String(r.body_sha256)===e.bodySha256&&Number(r.body_bytes)===e.bodyBytes&&String(r.source_cohort_id)===e.sourceCohortId;}))return{pass:false,status:"ROUTE_BODY_INTEGRITY_MISMATCH",d1Subqueries:q};const observed=await sha256Hex(JSON.stringify({compositionVersion:"v8004",corpusVersion:"v8004",shardNumber:batch.shardNumber,entries}));const prior=await controlDb.prepare(`SELECT expected_sha256,row_count,verified FROM ${STEP8G_ROUTE_RECEIPT_TABLE} WHERE composition_version='v8004' AND batch_id=? LIMIT 1`).bind(batch.batchId).first();q++;if(prior){if(String(prior.expected_sha256)!==observed||Number(prior.row_count)!==batch.rowCount)return{pass:false,status:"ROUTE_RECEIPT_CONFLICT",d1Subqueries:q};const vr=await verifyRoutes(controlDb,entries);q++;if(!vr.pass)return{pass:false,status:"ROUTE_RECEIPT_ROW_MISMATCH",d1Subqueries:q};if(Number(prior.verified)===1)return{pass:true,skipped:true,status:"VERIFIED_IDEMPOTENT_SKIP",rowCount:vr.rowCount,d1Subqueries:q};const promoted=await controlDb.prepare(`UPDATE ${STEP8G_ROUTE_RECEIPT_TABLE} SET verified=1 WHERE composition_version='v8004' AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(batch.batchId,observed,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"RECOVERED_UNKNOWN_COMMIT_AND_VERIFIED",rowCount:vr.rowCount,d1Subqueries:q};}const statements=entries.map(e=>controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_TABLE} (composition_version,recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes) VALUES ('v8004',?,?,?,?,?,?)`).bind(e.recipeId,e.corpusVersion,e.shardNumber,e.sourceCohortId,e.bodySha256,e.bodyBytes));statements.push(controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_RECEIPT_TABLE} (composition_version,batch_id,expected_sha256,row_count,verified) VALUES ('v8004',?,?,?,0)`).bind(batch.batchId,observed,batch.rowCount));try{await controlDb.batch(statements);}catch{return{pass:false,status:"WRITE_ERROR_UNKNOWN_COMMIT_STATE",d1Subqueries:q+statements.length};}q+=statements.length;const vr=await verifyRoutes(controlDb,entries);q++;if(!vr.pass)return{pass:false,status:"POST_WRITE_ROUTE_VERIFICATION_MISMATCH",d1Subqueries:q};const promoted=await controlDb.prepare(`UPDATE ${STEP8G_ROUTE_RECEIPT_TABLE} SET verified=1 WHERE composition_version='v8004' AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(batch.batchId,observed,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"WRITTEN_AND_EXACTLY_VERIFIED",rowCount:vr.rowCount,d1Subqueries:q};}

export async function readStep8GV8004RouteProgress(controlDb){const parent=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8004' AND corpus_version IN ('v8001','v8002','v8003')`).first();const child=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8004' AND corpus_version='v8004'`).first();const receipts=await controlDb.prepare(`SELECT batch_id,expected_sha256,row_count,verified FROM ${STEP8G_ROUTE_RECEIPT_TABLE} WHERE composition_version='v8004' ORDER BY batch_id`).all();let q=3;const completed=[],pending=[],conflicts=[];for(const row of receipts?.results||[]){const id=String(row.batch_id||""),expected=routeBatchById.get(id);if(!expected||Number(row.row_count)!==expected.rowCount||!/^[0-9a-f]{64}$/.test(String(row.expected_sha256||"")))conflicts.push({batchId:id,reason:"ROUTE_RECEIPT_MISMATCH"});else if(Number(row.verified)===1)completed.push(id);else pending.push(id);}const seen=new Set([...completed,...pending]),missing=routeBatches.map(b=>b.batchId).filter(id=>!seen.has(id)),receiptRows=completed.reduce((sum,id)=>sum+routeBatchById.get(id).rowCount,0),parentRows=Number(parent?.c||0),childRows=Number(child?.c||0);return{pass:conflicts.length===0,parentRouteCount:parentRows,newRouteCount:childRows,verifiedReceiptRowCount:receiptRows,verifiedBatchCount:completed.length,completeVerified:conflicts.length===0&&!pending.length&&!missing.length&&parentRows===STEP8G_V8004_EXPECTED_PARENT_ROUTE_COUNT&&childRows===STEP8G_V8004_EXPECTED_RECIPE_COUNT&&receiptRows===STEP8G_V8004_EXPECTED_RECIPE_COUNT&&completed.length===routeBatches.length,totalRouteCount:parentRows+childRows,missingBatchIds:missing,conflicts,fullCorpusScans:0,d1Subqueries:q};}

export async function activateStep8GV8004Pointer(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(pointer.activeVersion===STEP8G_V8004_CORPUS_VERSION)return{pass:true,skipped:true,status:"ALREADY_ACTIVE",previousVersion:pointer.previousVersion,d1Subqueries:q};if(pointer.activeVersion!==STEP8G_V8004_PARENT_VERSION)return{pass:false,status:"PARENT_NOT_ACTIVE",activeVersion:pointer.activeVersion,d1Subqueries:q};const result=await controlDb.prepare(`UPDATE ${STEP8G_POINTER_TABLE} SET previous_version=active_version,active_version='v8004',manifest_sha256=?,updated_at=CURRENT_TIMESTAMP WHERE scope=? AND active_version='v8003'`).bind(descriptor.layerManifestSha256,STEP8G_POINTER_SCOPE).run();q++;return{pass:Number(result?.meta?.changes??0)===1,status:"ACTIVATED_V8004",previousVersion:"v8003",d1Subqueries:q};}
export async function rollbackStep8GV8004Pointer(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(pointer.activeVersion===STEP8G_V8004_PARENT_VERSION)return{pass:true,skipped:true,status:"ALREADY_ROLLED_BACK",d1Subqueries:q};if(pointer.activeVersion!==STEP8G_V8004_CORPUS_VERSION)return{pass:false,status:"ROLLBACK_SOURCE_NOT_ACTIVE",activeVersion:pointer.activeVersion,d1Subqueries:q};const result=await controlDb.prepare(`UPDATE ${STEP8G_POINTER_TABLE} SET previous_version=active_version,active_version='v8003',manifest_sha256=?,updated_at=CURRENT_TIMESTAMP WHERE scope=? AND active_version='v8004'`).bind(STEP8G_V8003_RUNTIME_DESCRIPTOR.layerManifestSha256,STEP8G_POINTER_SCOPE).run();q++;return{pass:Number(result?.meta?.changes??0)===1,status:"ROLLED_BACK_TO_V8003",d1Subqueries:q};}
