import { execFileSync } from "node:child_process";
import { readFile } from "node:fs/promises";
import { resolve } from "node:path";
import { recipeDatabaseShardForId } from "./corpus-scale-step7a-core.mjs";
import { STEP8G_V8007_RUNTIME_DESCRIPTOR } from "../data/generated/step8g/v8007-runtime-descriptor.mjs";
import { STEP8G_V8007_EXPECTED_RECIPE_COUNT,expectedStep8GV8007BodyBatchIds,expectedStep8GV8007RouteBatchIds,materializeStep8GV8007IncomingBodyBatch,publicStep8GV8007RouteBatch } from "../src/server/step8g-v8007-live-runtime.mjs";
import { sha256Hex } from "../src/server/step8b-live.mjs";
const COLLECTION="cozinha-portuguesa",TITLE="Cozinheiro moderno, ou nova arte de cozinha",AUTHOR="Lucas Rigaud",YEAR="1785",SOURCE_URL="https://archive.org/details/b28764626";
function parseArgs(argv){const out={candidate:null};for(const arg of argv)if(arg.startsWith("--candidate="))out.candidate=arg.slice(12);if(!out.candidate)throw new Error("--candidate=<checked-out source path> is required");return out;}
const commitAt=path=>execFileSync("git",["-C",path,"rev-parse","HEAD"],{encoding:"utf8"}).trim();
function slug(v){return String(v??"").normalize("NFKD").replace(/\p{M}+/gu,"").toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_+|_+$/g,"");}
function chunk(values,size){const out=[];for(let i=0;i<values.length;i+=size)out.push(values.slice(i,i+size));return out;}
const args=parseArgs(process.argv.slice(2)),root=resolve(args.candidate);if(commitAt(root)!==STEP8G_V8007_RUNTIME_DESCRIPTOR.sourceCommit)throw new Error("ORA_SOURCE_PIN_MISMATCH");
const rawLines=(await readFile(resolve(root,`collections/${COLLECTION}/recipes.jsonl`),"utf8")).split(/\r?\n/).filter(Boolean),rows=[];
for(const [sourceOrdinal,rawJson] of rawLines.entries()){const row=JSON.parse(rawJson);if(String(row.collection??"")!==COLLECTION||String(row.source_title??"")!==TITLE||String(row.author??"")!==AUTHOR||String(row.source_year??"")!==YEAR||String(row.source_url??"")!==SOURCE_URL||String(row.license??"")!=="public-domain")continue;const ordinal=rows.length,recipeId=`ora_rigaud_1785_${slug(row.slug||row.title||String(ordinal))}`;rows.push({ordinal,sourceOrdinal,rawJson,recipeId,shardNumber:recipeDatabaseShardForId(recipeId,2)});}
if(rows.length!==STEP8G_V8007_EXPECTED_RECIPE_COUNT)throw new Error(`RIGAUD_EXPECTED_${STEP8G_V8007_EXPECTED_RECIPE_COUNT}_GOT_${rows.length}`);if(new Set(rows.map(r=>r.recipeId)).size!==rows.length)throw new Error("SOURCE_RECIPE_ID_UNIVERSE_NOT_UNIQUE");
const byShard=[rows.filter(r=>r.shardNumber===0),rows.filter(r=>r.shardNumber===1)],bodyBatches=[];for(const shardNumber of [0,1])for(const [batchNumber,batchRows] of chunk(byShard[shardNumber],10).entries())bodyBatches.push({batchId:`s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,shardNumber,rows:batchRows});
if(JSON.stringify(bodyBatches.map(b=>b.batchId))!==JSON.stringify(expectedStep8GV8007BodyBatchIds()))throw new Error("BODY_BATCH_LAYOUT_MISMATCH");
const routes=[];for(const batch of bodyBatches){const result=await materializeStep8GV8007IncomingBodyBatch({batchId:batch.batchId,sourceEntries:batch.rows.map(({ordinal,sourceOrdinal,rawJson})=>({ordinal,sourceOrdinal,rawJson}))});if(!result.pass)throw new Error(`${batch.batchId}:${result.reason}`);routes.push(...result.batch.entries.map(e=>({recipeId:e.recipeId,corpusVersion:"v8007",shardNumber:batch.shardNumber,sourceCohortId:e.sourceCohortId,bodySha256:e.bodySha256,bodyBytes:e.bodyBytes})));}
if(routes.length!==789||new Set(routes.map(r=>r.recipeId)).size!==789)throw new Error("ROUTE_UNIVERSE_MISMATCH");const routeBatchIds=[];for(const shardNumber of [0,1]){const shardRoutes=routes.filter(r=>r.shardNumber===shardNumber).sort((a,b)=>a.recipeId.localeCompare(b.recipeId));for(const [batchNumber,entries] of chunk(shardRoutes,10).entries()){const batchId=`route-v8007-s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,frozen=publicStep8GV8007RouteBatch(batchId);if(!frozen)throw new Error(`UNKNOWN_FROZEN_ROUTE_BATCH_${batchId}`);const observed=await sha256Hex(JSON.stringify({compositionVersion:"v8007",corpusVersion:"v8007",shardNumber,entries}));if(observed!==frozen.expectedSha256)throw new Error(`ROUTE_BATCH_FINGERPRINT_MISMATCH_${batchId}`);routeBatchIds.push(batchId);}}
if(JSON.stringify(routeBatchIds)!==JSON.stringify(expectedStep8GV8007RouteBatchIds()))throw new Error("ROUTE_BATCH_LAYOUT_MISMATCH");process.stdout.write(`${JSON.stringify({pass:true,sourceCommit:STEP8G_V8007_RUNTIME_DESCRIPTOR.sourceCommit,recipeCount:789,bodyBatchCount:bodyBatches.length,routeBatchCount:routeBatchIds.length,shardRows:byShard.map(x=>x.length),parentRouteCount:2906,composedRouteCount:3695,restartSafeResume:true,terminal:"STEP_8G_ORA_RIGAUD_V8007_LIVE_PAYLOAD_PARITY_PASS"},null,2)}\n`);
