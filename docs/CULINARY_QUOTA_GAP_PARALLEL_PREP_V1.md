# Culinary Quota-Gap Parallel Preparation V1

Date: **2026-09-25**

Status: **AUTHORIZED / ACTIVE / ZERO-D1 PREPARATION**

## Purpose

Use the P1 Cloudflare D1 daily-quota hold productively without weakening the protected-corpus gate.

This lane is preparation only. It consumes repository-frozen evidence and the current 85-recipe public runtime. It does not query protected D1, select protected recipe IDs, hydrate protected bodies, change recommendation behavior, or widen authority.

## Work authorized now

1. **C0 Golden-85 harness** — exercise the exact current 85-recipe public runtime and record deterministic coverage/state inputs for later Brain calibration.
2. **P2 metadata-usability harness** — transform the already-frozen v8018 normalization authority summary into an explicit dimension-level blocker table.
3. **C1 sampling design** — freeze a 500-slot source-cohort quota plan using the frozen 19,268-recipe cohort counts, with at least one slot per cohort. Exact protected IDs are not selected yet.

## P1 boundary

P1 remains incomplete while the private index is only partially built. Exact C1 protected selection/execution, live/full-v8018 P2 measurement, C2 full classification and P3 recommendation admission remain blocked until P1 reaches terminal live acceptance.

## C1 secondary strata after P1

After P1 passes, exact identities must be chosen within the frozen source-cohort quotas while considering ingredient-identity readiness, known/unknown metadata, dish/category diversity, historical/modern context where available, and the known structural-exception boundaries. Selection must be frozen before evaluation.

## Hard boundaries

- protected D1 reads: **0**
- protected D1 writes: **0**
- protected bodies read/exported/rewritten: **0**
- Knowledge Core writes: **0**
- public runtime change: **none**
- recommendation behavior/authority change: **none**
- paid infrastructure: **not authorized**
- third shard: **not authorized**
- Barbecue mutation: **not authorized**

The canonical machine contract is `config/culinary_quota_gap_parallel_prep_v1.json`.
