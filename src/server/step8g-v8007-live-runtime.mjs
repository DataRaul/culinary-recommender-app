import { STEP8G_V8007_RUNTIME_DESCRIPTOR as descriptor } from "../../data/generated/step8g/v8007-runtime-descriptor.mjs";
import { STEP8G_V8006_RUNTIME_DESCRIPTOR } from "../../data/generated/step8g/v8006-runtime-descriptor.mjs";
import { STEP8B_LIVE_SHARD_SPECS, STEP8B_RECEIPT_TABLE, STEP8B_RECEIPT_TABLE_SQL, STEP8B_RECIPE_TABLE, STEP8B_RECIPE_TABLE_SQL, sha256Hex } from "./step8b-live.mjs";
import { STEP8G_POINTER_SCOPE, STEP8G_POINTER_TABLE, STEP8G_POINTER_TABLE_SQL, STEP8G_ROUTE_RECEIPT_TABLE, STEP8G_ROUTE_RECEIPT_TABLE_SQL, STEP8G_ROUTE_TABLE, STEP8G_ROUTE_TABLE_SQL, readStep8GPointer } from "./step8g-live-runtime.mjs";

const encoder=new TextEncoder();
export const STEP8G_V8007_CORPUS_VERSION="v8007";
export const STEP8G_V8007_PARENT_VERSION="v8006";
export const STEP8G_V8007_SOURCE_COHORT_ID="ORA_RIGAUD_1785_PORTUGUESE_SOURCE_AE3BD2C";
export const STEP8G_V8007_EXPECTED_RECIPE_COUNT=789;
export const STEP8G_V8007_EXPECTED_BODY_BATCH_COUNT=79;
export const STEP8G_V8007_EXPECTED_ROUTE_COUNT=3695;
export const STEP8G_V8007_EXPECTED_PARENT_ROUTE_COUNT=2906;
export const STEP8G_V8007_MAX_ROWS_PER_BATCH=10;
export const STEP8G_V8007_MAX_PROTECTED_D1_SUBQUERIES=16;
export const STEP8G_V8007_LIVE_SHARD_SPECS=STEP8B_LIVE_SHARD_SPECS;
const SOURCE_REPOSITORY="AdamBouhmad/open-recipe-archive",SOURCE_COLLECTION="cozinha-portuguesa",SOURCE_TITLE="Cozinheiro moderno, ou nova arte de cozinha",SOURCE_AUTHOR="Lucas Rigaud",SOURCE_YEAR="1785",SOURCE_URL="https://archive.org/details/b28764626";
const PARENT_VERSIONS=["v8001","v8002","v8003","v8004","v8005","v8006"];
const bodyHashes=descriptor.bodyHashHexByShard.map(v=>v.match(/.{64}/g)||[]),routeHashes=descriptor.routeHashHexByShard.map(v=>v.match(/.{64}/g)||[]);
const bodyBatches=bodyHashes.flatMap((list,shardNumber)=>list.map((expectedSha256,batchNumber)=>{const remaining=Number(descriptor.bodyShardRows[shardNumber])-batchNumber*10;return{batchId:`s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,shardNumber,batchNumber,rowCount:Math.min(10,remaining),expectedSha256};}));
const routeBatches=descriptor.bodyShardRows.flatMap((rowCount,shardNumber)=>Array.from({length:Math.ceil(Number(rowCount)/10)},(_,batchNumber)=>({batchId:`route-v8007-s${String(shardNumber).padStart(2,"0")}-b${String(batchNumber).padStart(6,"0")}`,shardNumber,batchNumber,rowCount:Math.min(10,Number(rowCount)-batchNumber*10),expectedSha256:routeHashes[shardNumber]?.[batchNumber]})));
const bodyBatchById=new Map(bodyBatches.map(b=>[b.batchId,b])),routeBatchById=new Map(routeBatches.map(b=>[b.batchId,b]));
const bytes=v=>encoder.encode(String(v)).byteLength;
const bodyDescriptor=e=>({ordinal:Number(e.ordinal),recipeId:String(e.recipeId),bodySha256:String(e.bodySha256),bodyBytes:Number(e.bodyBytes),sourceCohortId:String(e.sourceCohortId)});
const routeDescriptor=e=>({recipeId:String(e.recipeId),corpusVersion:String(e.corpusVersion),shardNumber:Number(e.shardNumber),sourceCohortId:String(e.sourceCohortId),bodySha256:String(e.bodySha256),bodyBytes:Number(e.bodyBytes)});
function section(body,heading){const m=new RegExp(`^##\\s+${heading}\\s*$`,"im").exec(body);if(!m)return"";const rest=body.slice(m.index+m[0].length),next=/^##\s+/m.exec(rest);return(next?rest.slice(0,next.index):rest).trim();}
function parseSourceRow(row={}){const body=String(row.body??"");return{title:String(row.title??""),slug:String(row.slug??""),ingredients:section(body,"Ingredients").split(/\r?\n/).filter(l=>/^\s*[-*+]\s+/.test(l)).map(l=>l.replace(/^\s*[-*+]\s+/,"").trim()).filter(Boolean),directions:section(body,"(?:Directions|Instructions|Method)").split(/\r?\n/).filter(l=>/^\s*(?:\d+[.)]|[-*+])\s+/.test(l)).map(l=>l.replace(/^\s*(?:\d+[.)]|[-*+])\s+/,"").trim()).filter(Boolean)};}
function slug(v){return String(v??"").normalize("NFKD").replace(/\p{M}+/gu,"").toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_+|_+$/g,"");}
async function buildBody(rawJson,ordinal,sourceOrdinal){let row;try{row=JSON.parse(String(rawJson??""));}catch{throw new Error("SOURCE_JSON_INVALID");}const parsed=parseSourceRow(row);if(String(row.collection??"")!==SOURCE_COLLECTION||String(row.source_title??"")!==SOURCE_TITLE||String(row.author??"")!==SOURCE_AUTHOR||String(row.source_year??"")!==SOURCE_YEAR||String(row.source_url??"")!==SOURCE_URL||String(row.license??"")!=="public-domain"||!parsed.title||!parsed.ingredients.length||!parsed.directions.length)throw new Error("SOURCE_RECORD_RIGHTS_OR_STRUCTURE_MISMATCH");if(!Number.isInteger(sourceOrdinal)||sourceOrdinal<0)throw new Error("SOURCE_ROW_ORDINAL_INVALID");const sourceSlug=slug(parsed.slug||parsed.title||String(ordinal)),recipeId=`ora_rigaud_1785_${sourceSlug}`;if(!sourceSlug)throw new Error("SOURCE_RECIPE_ID_INVALID");const packet={schema:"STEP8G_ORA_RIGAUD_1785_PROTECTED_SOURCE_PACKET_V1",canonicalRecipeId:recipeId,source:{cohortId:STEP8G_V8007_SOURCE_COHORT_ID,repository:SOURCE_REPOSITORY,commit:descriptor.sourceCommit,path:`collections/${SOURCE_COLLECTION}/recipes.jsonl`,rowOrdinal:sourceOrdinal,sourceContentSha256:await sha256Hex(rawJson),sourceWork:SOURCE_TITLE,sourceAuthor:SOURCE_AUTHOR,sourceYear:SOURCE_YEAR,sourceUrl:SOURCE_URL,licenseId:"public-domain",historicalCollectionLabel:SOURCE_COLLECTION},sourceContent:{rawJson:String(rawJson),title:parsed.title,slug:parsed.slug,parsedIngredientsNonAuthoritative:parsed.ingredients,parsedDirectionsNonAuthoritative:parsed.directions},authority:{recommendationAdmissionAuthorized:false,publicRuntimeActivationAuthorized:false,ingredientOntologyAuthority:false,nutritionAuthority:false,dietaryAllergenAuthority:false,scalingAuthority:false,culturalAuthenticityAuthority:false,historicalSourceLabelOnly:true,knowledgeCoreWriteAuthorized:false}};const bodyJson=JSON.stringify(packet);return{recipeId,bodyJson,bodySha256:await sha256Hex(bodyJson),bodyBytes:bytes(bodyJson),sourceCohortId:STEP8G_V8007_SOURCE_COHORT_ID};}

export function publicStep8GV8007Summary(){return{contractVersion:"CORPUS_SCALE_STEP8G_ORA_RIGAUD_V8007_LIVE_V1",corpusVersion:"v8007",parentCorpusVersion:"v8006",sourceCohortId:STEP8G_V8007_SOURCE_COHORT_ID,sourceCommit:descriptor.sourceCommit,recipeCount:789,cumulativeRecipeCount:3695,bodyBatchCount:bodyBatches.length,newRouteBatchCount:routeBatches.length,shardCount:2,maxRowsPerBatch:10,maxProtectedD1Subqueries:16,layerManifestSha256:descriptor.layerManifestSha256,populationPlanSha256:descriptor.populationPlanSha256,publicRuntimeActivationAuthorized:false,recommendationAdmissionAuthorized:false,billingExpansionAuthorized:false,thirdShardAuthorized:false,culturalAuthenticityAuthorityImported:false,restartSafeResume:true};}
export const expectedStep8GV8007BodyBatchIds=()=>bodyBatches.map(b=>b.batchId);
export const expectedStep8GV8007RouteBatchIds=()=>routeBatches.map(b=>b.batchId);
export function publicStep8GV8007BodyBatch(id){const b=bodyBatchById.get(String(id||""));return b?{...b}:null;}
export function publicStep8GV8007RouteBatch(id){const b=routeBatchById.get(String(id||""));return b?{...b}:null;}
export async function materializeStep8GV8007IncomingBodyBatch(payload={}){const expected=bodyBatchById.get(String(payload.batchId||""));if(!expected)return{pass:false,reason:"UNKNOWN_BODY_BATCH_ID"};if(!Array.isArray(payload.sourceEntries)||payload.sourceEntries.length!==expected.rowCount)return{pass:false,reason:"BODY_BATCH_ROW_COUNT_MISMATCH"};const entries=[],ids=new Set();for(const incoming of payload.sourceEntries){const ordinal=Number(incoming?.ordinal),sourceOrdinal=Number(incoming?.sourceOrdinal);if(!Number.isInteger(ordinal)||ordinal<0||ordinal>=789)return{pass:false,reason:"SOURCE_ORDINAL_INVALID"};let built;try{built=await buildBody(String(incoming?.rawJson||""),ordinal,sourceOrdinal);}catch(error){return{pass:false,reason:error.message||"SOURCE_RECORD_REJECTED"};}if(ids.has(built.recipeId))return{pass:false,reason:"DUPLICATE_SOURCE_RECIPE_ID"};ids.add(built.recipeId);entries.push({ordinal,...built});}const observed=await sha256Hex(JSON.stringify({shardNumber:expected.shardNumber,batchNumber:expected.batchNumber,entries:entries.map(bodyDescriptor)}));if(observed!==expected.expectedSha256)return{pass:false,reason:"BODY_BATCH_FINGERPRINT_MISMATCH"};return{pass:true,batch:{...expected,entries}};}
export async function ensureStep8GV8007ShardSchema(db){await db.prepare(STEP8B_RECIPE_TABLE_SQL).run();await db.prepare(STEP8B_RECEIPT_TABLE_SQL).run();return{initialized:true,d1Subqueries:2};}
export async function initializeStep8GV8007ControlSchema(db){await db.prepare(STEP8G_POINTER_TABLE_SQL).run();await db.prepare(STEP8G_ROUTE_TABLE_SQL).run();await db.prepare(STEP8G_ROUTE_RECEIPT_TABLE_SQL).run();return{initialized:true,d1Subqueries:3};}
async function verifyBodyRows(db,batch){const ids=batch.entries.map(e=>e.recipeId),ph=ids.map(()=>"?").join(","),rows=await db.prepare(`SELECT ordinal,recipe_id,body_bytes,body_sha256,source_cohort_id FROM ${STEP8B_RECIPE_TABLE} WHERE corpus_version=? AND recipe_id IN (${ph})`).bind("v8007",...ids).all(),byId=new Map((rows?.results||[]).map(r=>[String(r.recipe_id),r]));return{pass:byId.size===batch.entries.length&&batch.entries.every(e=>{const r=byId.get(e.recipeId);return r&&Number(r.ordinal)===e.ordinal&&Number(r.body_bytes)===e.bodyBytes&&String(r.body_sha256)===e.bodySha256&&String(r.source_cohort_id)===e.sourceCohortId;}),rowCount:byId.size,d1Subqueries:1};}
export async function writeStep8GV8007BodyBatch(db,batch){const prior=await db.prepare(`SELECT expected_sha256,row_count,verified FROM ${STEP8B_RECEIPT_TABLE} WHERE corpus_version=? AND batch_id=? LIMIT 1`).bind("v8007",batch.batchId).first();let q=1;if(prior){if(String(prior.expected_sha256)!==batch.expectedSha256||Number(prior.row_count)!==batch.rowCount)return{pass:false,status:"BODY_RECEIPT_CONFLICT",d1Subqueries:q};const rows=await verifyBodyRows(db,batch);q++;if(!rows.pass)return{pass:false,status:"BODY_RECEIPT_ROW_MISMATCH",d1Subqueries:q};if(Number(prior.verified)===1)return{pass:true,skipped:true,status:"VERIFIED_IDEMPOTENT_SKIP",rowCount:rows.rowCount,d1Subqueries:q};const promoted=await db.prepare(`UPDATE ${STEP8B_RECEIPT_TABLE} SET verified=1 WHERE corpus_version=? AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind("v8007",batch.batchId,batch.expectedSha256,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"RECOVERED_UNKNOWN_COMMIT_AND_VERIFIED",rowCount:rows.rowCount,d1Subqueries:q};}if(typeof db.batch!=="function")return{pass:false,status:"D1_BATCH_UNAVAILABLE",d1Subqueries:q};const statements=batch.entries.map(e=>db.prepare(`INSERT OR ABORT INTO ${STEP8B_RECIPE_TABLE} (corpus_version,ordinal,recipe_id,body_json,body_bytes,body_sha256,source_cohort_id) VALUES (?,?,?,?,?,?,?)`).bind("v8007",e.ordinal,e.recipeId,e.bodyJson,e.bodyBytes,e.bodySha256,e.sourceCohortId));statements.push(db.prepare(`INSERT OR ABORT INTO ${STEP8B_RECEIPT_TABLE} (corpus_version,batch_id,expected_sha256,row_count,verified) VALUES (?,?,?,?,0)`).bind("v8007",batch.batchId,batch.expectedSha256,batch.rowCount));try{await db.batch(statements);}catch{return{pass:false,status:"WRITE_ERROR_UNKNOWN_COMMIT_STATE",d1Subqueries:q+statements.length};}q+=statements.length;const rows=await verifyBodyRows(db,batch);q++;if(!rows.pass)return{pass:false,status:"POST_WRITE_ROW_VERIFICATION_MISMATCH",d1Subqueries:q};const promoted=await db.prepare(`UPDATE ${STEP8B_RECEIPT_TABLE} SET verified=1 WHERE corpus_version=? AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind("v8007",batch.batchId,batch.expectedSha256,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"WRITTEN_AND_EXACTLY_VERIFIED",rowCount:rows.rowCount,d1Subqueries:q};}
export async function readStep8GV8007BodyProgress(shardDbs){const completed=[],pending=[],conflicts=[];let q=0;for(const spec of STEP8G_V8007_LIVE_SHARD_SPECS){const rows=await shardDbs[spec.shardNumber].prepare(`SELECT batch_id,expected_sha256,row_count,verified FROM ${STEP8B_RECEIPT_TABLE} WHERE corpus_version=? ORDER BY batch_id`).bind("v8007").all();q++;for(const row of rows?.results||[]){const id=String(row.batch_id||""),expected=bodyBatchById.get(id);if(!expected||expected.shardNumber!==spec.shardNumber||String(row.expected_sha256)!==expected.expectedSha256||Number(row.row_count)!==expected.rowCount)conflicts.push({batchId:id,reason:"BODY_RECEIPT_MISMATCH"});else if(Number(row.verified)===1)completed.push(id);else pending.push(id);}}const seen=new Set([...completed,...pending]),missing=bodyBatches.map(b=>b.batchId).filter(id=>!seen.has(id)),verifiedRowCount=completed.reduce((s,id)=>s+bodyBatchById.get(id).rowCount,0);return{pass:conflicts.length===0,completeVerified:conflicts.length===0&&!pending.length&&!missing.length&&completed.length===79&&verifiedRowCount===789,completedBatchIds:completed.sort(),pendingVerificationBatchIds:pending.sort(),missingBatchIds:missing,conflicts,verifiedBatchCount:completed.length,verifiedRowCount,expectedBatchCount:79,expectedRecipeCount:789,fullCorpusScans:0,d1Subqueries:q};}
export async function copyStep8GV8007ParentRoutes(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(!["v8006","v8007"].includes(pointer.activeVersion))return{pass:false,status:"V8006_PARENT_OR_V8007_ACTIVE_REQUIRED",activeVersion:pointer.activeVersion,d1Subqueries:q};const parent=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8006'`).first();q++;if(Number(parent?.c||0)!==2906)return{pass:false,status:"V8006_ROUTE_BASELINE_NOT_EXACT",rowCount:Number(parent?.c||0),d1Subqueries:q};const existing=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8007' AND corpus_version IN ('v8001','v8002','v8003','v8004','v8005','v8006')`).first();q++;if(Number(existing?.c||0)===2906)return{pass:true,skipped:true,status:"PARENT_ROUTES_IDEMPOTENT_SKIP",rowCount:2906,d1Subqueries:q};if(Number(existing?.c||0)!==0)return{pass:false,status:"PARENT_ROUTE_PARTIAL_CONFLICT",rowCount:Number(existing?.c||0),d1Subqueries:q};if(pointer.activeVersion!=="v8006")return{pass:false,status:"V8007_ACTIVE_WITHOUT_EXACT_PARENT_ROUTES",d1Subqueries:q};await controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_TABLE} (composition_version,recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes) SELECT 'v8007',recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8006'`).run();q++;const after=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8007' AND corpus_version IN ('v8001','v8002','v8003','v8004','v8005','v8006')`).first();q++;const count=Number(after?.c||0);return{pass:count===2906,status:count===2906?"PARENT_ROUTES_COPIED_AND_VERIFIED":"PARENT_ROUTE_COPY_MISMATCH",rowCount:count,d1Subqueries:q};}
async function verifyRoutes(controlDb,entries){const ids=entries.map(e=>e.recipeId),ph=ids.map(()=>"?").join(","),rows=await controlDb.prepare(`SELECT recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8007' AND recipe_id IN (${ph})`).bind(...ids).all(),byId=new Map((rows?.results||[]).map(r=>[String(r.recipe_id),r]));return{pass:byId.size===entries.length&&entries.every(e=>{const r=byId.get(e.recipeId);return r&&String(r.corpus_version)===e.corpusVersion&&Number(r.shard_number)===e.shardNumber&&String(r.source_cohort_id)===e.sourceCohortId&&String(r.body_sha256)===e.bodySha256&&Number(r.body_bytes)===e.bodyBytes;}),rowCount:byId.size,d1Subqueries:1};}
export async function writeStep8GV8007RouteBatch(controlDb,shardDbs,payload={}){const batch=routeBatchById.get(String(payload.batchId||""));if(!batch)return{pass:false,status:"UNKNOWN_ROUTE_BATCH_ID",d1Subqueries:0};if(!Array.isArray(payload.entries)||payload.entries.length!==batch.rowCount)return{pass:false,status:"ROUTE_BATCH_ROW_COUNT_MISMATCH",d1Subqueries:0};const entries=payload.entries.map(routeDescriptor),ids=entries.map(e=>e.recipeId);if(new Set(ids).size!==ids.length||JSON.stringify(ids)!==JSON.stringify([...ids].sort())||entries.some(e=>e.corpusVersion!=="v8007"||e.shardNumber!==batch.shardNumber||!e.recipeId||e.sourceCohortId!==STEP8G_V8007_SOURCE_COHORT_ID||!/^[0-9a-f]{64}$/.test(e.bodySha256)||e.bodyBytes<=0))return{pass:false,status:"ROUTE_BATCH_DESCRIPTOR_INVALID",d1Subqueries:0};const observed=await sha256Hex(JSON.stringify({compositionVersion:"v8007",corpusVersion:"v8007",shardNumber:batch.shardNumber,entries}));if(observed!==batch.expectedSha256)return{pass:false,status:"ROUTE_BATCH_FINGERPRINT_MISMATCH",d1Subqueries:0};const ph=ids.map(()=>"?").join(","),bodies=await shardDbs[batch.shardNumber].prepare(`SELECT recipe_id,body_sha256,body_bytes,source_cohort_id FROM ${STEP8B_RECIPE_TABLE} WHERE corpus_version='v8007' AND recipe_id IN (${ph})`).bind(...ids).all();let q=1;const byId=new Map((bodies?.results||[]).map(r=>[String(r.recipe_id),r]));if(byId.size!==entries.length||!entries.every(e=>{const r=byId.get(e.recipeId);return r&&String(r.body_sha256)===e.bodySha256&&Number(r.body_bytes)===e.bodyBytes&&String(r.source_cohort_id)===e.sourceCohortId;}))return{pass:false,status:"ROUTE_BODY_INTEGRITY_MISMATCH",d1Subqueries:q};const prior=await controlDb.prepare(`SELECT expected_sha256,row_count,verified FROM ${STEP8G_ROUTE_RECEIPT_TABLE} WHERE composition_version='v8007' AND batch_id=? LIMIT 1`).bind(batch.batchId).first();q++;if(prior){if(String(prior.expected_sha256)!==observed||Number(prior.row_count)!==batch.rowCount)return{pass:false,status:"ROUTE_RECEIPT_CONFLICT",d1Subqueries:q};const verified=await verifyRoutes(controlDb,entries);q++;if(!verified.pass)return{pass:false,status:"ROUTE_RECEIPT_ROW_MISMATCH",d1Subqueries:q};if(Number(prior.verified)===1)return{pass:true,skipped:true,status:"VERIFIED_IDEMPOTENT_SKIP",rowCount:verified.rowCount,d1Subqueries:q};const promoted=await controlDb.prepare(`UPDATE ${STEP8G_ROUTE_RECEIPT_TABLE} SET verified=1 WHERE composition_version='v8007' AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(batch.batchId,observed,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"RECOVERED_UNKNOWN_COMMIT_AND_VERIFIED",rowCount:verified.rowCount,d1Subqueries:q};}const statements=entries.map(e=>controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_TABLE} (composition_version,recipe_id,corpus_version,shard_number,source_cohort_id,body_sha256,body_bytes) VALUES ('v8007',?,?,?,?,?,?)`).bind(e.recipeId,e.corpusVersion,e.shardNumber,e.sourceCohortId,e.bodySha256,e.bodyBytes));statements.push(controlDb.prepare(`INSERT OR ABORT INTO ${STEP8G_ROUTE_RECEIPT_TABLE} (composition_version,batch_id,expected_sha256,row_count,verified) VALUES ('v8007',?,?,?,0)`).bind(batch.batchId,observed,batch.rowCount));try{await controlDb.batch(statements);}catch{return{pass:false,status:"WRITE_ERROR_UNKNOWN_COMMIT_STATE",d1Subqueries:q+statements.length};}q+=statements.length;const verified=await verifyRoutes(controlDb,entries);q++;if(!verified.pass)return{pass:false,status:"POST_WRITE_ROUTE_VERIFICATION_MISMATCH",d1Subqueries:q};const promoted=await controlDb.prepare(`UPDATE ${STEP8G_ROUTE_RECEIPT_TABLE} SET verified=1 WHERE composition_version='v8007' AND batch_id=? AND expected_sha256=? AND row_count=? AND verified=0`).bind(batch.batchId,observed,batch.rowCount).run();q++;return{pass:Number(promoted?.meta?.changes??0)===1,status:"WRITTEN_AND_EXACTLY_VERIFIED",rowCount:verified.rowCount,d1Subqueries:q};}
export async function readStep8GV8007RouteProgress(controlDb){const parent=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8007' AND corpus_version IN ('v8001','v8002','v8003','v8004','v8005','v8006')`).first(),child=await controlDb.prepare(`SELECT COUNT(*) AS c FROM ${STEP8G_ROUTE_TABLE} WHERE composition_version='v8007' AND corpus_version='v8007'`).first(),receipts=await controlDb.prepare(`SELECT batch_id,expected_sha256,row_count,verified FROM ${STEP8G_ROUTE_RECEIPT_TABLE} WHERE composition_version='v8007' ORDER BY batch_id`).all(),completed=[],pending=[],conflicts=[];for(const row of receipts?.results||[]){const id=String(row.batch_id||""),expected=routeBatchById.get(id);if(!expected||String(row.expected_sha256)!==expected.expectedSha256||Number(row.row_count)!==expected.rowCount)conflicts.push({batchId:id,reason:"ROUTE_RECEIPT_MISMATCH"});else if(Number(row.verified)===1)completed.push(id);else pending.push(id);}const seen=new Set([...completed,...pending]),missing=routeBatches.map(b=>b.batchId).filter(id=>!seen.has(id)),receiptRows=completed.reduce((s,id)=>s+routeBatchById.get(id).rowCount,0),parentRows=Number(parent?.c||0),childRows=Number(child?.c||0);return{pass:conflicts.length===0,parentRouteCount:parentRows,newRouteCount:childRows,verifiedReceiptRowCount:receiptRows,verifiedBatchCount:completed.length,completeVerified:conflicts.length===0&&!pending.length&&!missing.length&&parentRows===2906&&childRows===789&&receiptRows===789&&completed.length===79,totalRouteCount:parentRows+childRows,missingBatchIds:missing,conflicts,fullCorpusScans:0,d1Subqueries:3};}
export async function activateStep8GV8007Pointer(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(pointer.activeVersion==="v8007")return{pass:true,skipped:true,status:"ALREADY_ACTIVE",previousVersion:pointer.previousVersion,d1Subqueries:q};if(pointer.activeVersion!=="v8006")return{pass:false,status:"PARENT_NOT_ACTIVE",activeVersion:pointer.activeVersion,d1Subqueries:q};const result=await controlDb.prepare(`UPDATE ${STEP8G_POINTER_TABLE} SET previous_version=active_version,active_version='v8007',manifest_sha256=?,updated_at=CURRENT_TIMESTAMP WHERE scope=? AND active_version='v8006'`).bind(descriptor.layerManifestSha256,STEP8G_POINTER_SCOPE).run();q++;return{pass:Number(result?.meta?.changes??0)===1,status:"ACTIVATED_V8007",previousVersion:"v8006",d1Subqueries:q};}
export async function rollbackStep8GV8007Pointer(controlDb){const pointer=await readStep8GPointer(controlDb);let q=pointer.d1Subqueries;if(pointer.activeVersion==="v8006")return{pass:true,skipped:true,status:"ALREADY_ROLLED_BACK",d1Subqueries:q};if(pointer.activeVersion!=="v8007")return{pass:false,status:"ROLLBACK_SOURCE_NOT_ACTIVE",activeVersion:pointer.activeVersion,d1Subqueries:q};const result=await controlDb.prepare(`UPDATE ${STEP8G_POINTER_TABLE} SET previous_version=active_version,active_version='v8006',manifest_sha256=?,updated_at=CURRENT_TIMESTAMP WHERE scope=? AND active_version='v8007'`).bind(STEP8G_V8006_RUNTIME_DESCRIPTOR.layerManifestSha256,STEP8G_POINTER_SCOPE).run();q++;return{pass:Number(result?.meta?.changes??0)===1,status:"ROLLED_BACK_TO_V8006",d1Subqueries:q};}
